// Shared secret with urs-android's watch-relay (WatchRelayToken.kt) — must
// match exactly. Masked out of git the same way as app.json's appId (see
// committed content always
// shows the placeholder below, a per-clone smudge/clean filter swaps in the
// real value on checkout.
export const RELAY_TOKEN = '0'
